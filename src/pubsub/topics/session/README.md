# Session Topic

- **Topic ID**: `session` — cash register session (not HTTP session)

## Events

- `set-active-session` — `{ session: CFSession }`
- `get-active-session` — `{ session: CFSession | null }`
